services:
  - type: web
    name: gutter-pricing-api
    runtime: docker
    plan: free
    healthCheckPath: /health
    dockerCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --log-level info --preload